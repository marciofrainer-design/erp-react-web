# Variáveis de ambiente — copie este arquivo para .env e ajuste os valores
VITE_DATASNAP_URL=http://localhost:3000

# Ative para rodar o frontend sem backend (usa dados gerados com faker)
# VITE_MOCK_MODE=true
